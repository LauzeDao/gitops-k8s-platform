replicaCount: 1

config:
  baseURL: /headlamp
  inCluster: true
  oidc:
    externalSecret:
      enabled: false

service:
  type: ClusterIP
ingress:
  enabled: false

serviceAccount:
  create: true

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 256Mi
