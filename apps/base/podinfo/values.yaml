replicaCount: 1

image:
  repository: ghcr.io/stefanprodan/podinfo
  tag: 6.14.0 # {"$imagepolicy": "tools:podinfo:tag"}

service:
  type: ClusterIP

ingress:
  enabled: false

resources:
  requests:
    cpu: 50m
    memory: 32Mi
  limits:
    cpu: 200m
    memory: 128Mi
